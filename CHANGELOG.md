# Changelog

## Unreleased

- N-Back 每个 2 秒试次改为字母显示 1.5 秒、随后 0.5 秒注视十字；M1 试次注视十字与 M4A 试次加号统一缩短为 0.5 秒，M1/M4A 自动计时相应减少；
- 首页新增“跳过 M0”选项：同一天、同一次佩戴内连续批次可跳过后续批次的 M0 基线，Marker 写入 `m0_skipped`；跨日或重新佩戴后仍须重新执行完整 M0；
- 全屏任务页改为深色低亮度主题，顶部新增模块标题与步骤进度条，倒计时改用强调色，层次更清晰；
- 修复 `event_codes.csv` 中 deviceqc 事件名与实际 Marker 不一致的问题（`blink`/`jaw_clench`/`head_left`/`head_right`/`head_nod`/`head_cancel`），移除从未发送的 121/131 结束码；
- 修复 RCS 模式下进入下一模块时重新解析端口文本可能抛出异常的问题，现复用启动时已校验的端口；
- 计时节拍由 100 ms 加密至 50 ms，试次间隔抖动减半；N-Back 颜色反馈只在字母显示阶段给出，注视十字阶段的晚响应不再覆盖“+”（Marker 记 `feedback_shown=false`），且反馈显示后试次最后 0.5 秒仍会切换回注视十字。

## 0.8.0

- 新增 Windows x64 与 macOS ARM64 独立桌面版构建、冻结程序自检、架构校验、SHA-256 及 GitHub Release 工作流，并修复冻结应用的图片资源定位；
- 启动脚本默认关闭短流程；首页新增两批/三批采集预设，一键选择含当次 M0 的模块组合，并将批次与实际计时模式写入 Marker；
- 修复 N-Back 空格键因底部按钮残留焦点而提前推进步骤的问题；计时回调现绑定步骤代际，步骤完成逻辑幂等，旧回调不能污染下一试次；
- EEG 状态条新增 BSense ±375000 贴轨/削顶检测；M1 明显动作候选自动写入 `mi_motion_warning`，Run 评分增加实验员可见动作记录；
- M2 开始前新增 60 秒安静恢复及 KSS、精神疲劳、继续条件确认，首页对正式采集使用由易到难顺序给出混杂警告；
- 内置录制器启动后立即尝试 LSL 时钟偏移，失败时快速重试，并在诊断日志汇总偏移数量、失败次数、实测采样率和原始时间戳倒退数；
- 闭眼开始、组间休息开始新增离线中文女声提示，模块与区块开始保留边界提示，所有播放继续写入 `audio_cue` Marker；
- 延长模块开始、闭眼前后、M1 Run、M2/M3B 区块及 M3A 动作组的过渡时间，不改变有效分析窗口的持续时间；
- 全屏任务页新增 EEG、fNIRS、Motion 断流/实测采样率/恒定通道状态条，并可直接打开完整实时波形；
- 放大全屏底部继续、中止和质量标记按钮，四个操作入口等宽铺满，便于触控操作；
- 所有睁眼静息与恢复段统一显示中央注视十字，文字说明移到下方指导区；
- N-Back 每个 2 秒试次改为字母显示 0.5 秒、随后显示 1.5 秒注视十字；0-back 非目标池排除 `X`，解决连续刺激边界不清及同一 `X` 被赋予冲突标签的问题；
- 增加可选 N-Back 正确/错误颜色反馈，默认关闭且明确只用于练习/联调，Marker 记录是否显示反馈；
- M1 增加操作员确认的实际握拳到本体感觉想象引导练习，并把该段标为不进入正式分析；
- 提示音生成工具支持 `--missing-only`，新增协议、语音资源、N-Back 时序与恒定通道测试。

## 0.7.0

- 按 Fp1/Fp2 EEG 与额部 8 通道 fNIRS 的能力边界重新定位 M1、M4A、M4B 为探索性模块，移除预设准确率与单次 fNIRS 目标判定假设；
- M0 增加 30 秒自然呼吸稳定段，并明确不把深呼吸当作 fNIRS 零点或漂移校准；
- M1 分离准备提示、有效想象开始和试次结束 Marker，增加每 Run 主观成功度与努力度评分；
- M2 增加任务结束和恢复结束 Marker，保留逐刺激反应时、正确性和 60 秒恢复段；
- M3A 只标记预期运动伪迹候选窗口，不自动生成 EEG/fNIRS 污染真值；
- M3B 改用 1–9 KSS，增加分段边界、段内位置和 30 秒任务后恢复；
- M4A 明确为外部提示后的意图范式并增加模块级评分；M4B 改为串行目标注意，不再使用右手运动想象；
- 新增 M5 结构化结束问卷，记录困倦、任务难度、设备舒适度和不适事件；
- 数据文档区分原始 735/850 nm 光学流与派生 HbO/HbR，并要求所有采样率按 XDF 时间戳实测；
- 新增协议边界、Marker 语义和问卷完整性测试。

## 0.6.0

- 新增跨平台内置 LSL-to-XDF 录制后端，默认运行不再依赖 LabRecorder；
- 支持数值、字符串和 int64 LSL 流，记录原始时间戳、时钟偏移、边界块和流尾；
- 每个模块开始时强制检查六类 BioMultiLite 数值流、厂商 Marker 和实验 Marker；
- 录制页提供内置 XDF、LabRecorder RCS 和 LabRecorder 手动三种模式；
- 增加 XDF 二进制结构、录制生命周期及实验 Marker 必需流测试；
- 使用独立 `pyxdf` 读取器完成数值流与字符串 Marker 兼容验证；
- 修复 macOS 下后台扫描线程直接调用 Tk，导致 LSL 已发现但界面可能停留在扫描状态的问题；
- 所有录制准备、手动验证和停止结果统一通过主线程队列更新 Tk，避免 macOS 后台线程调度卡死；
- 内置录制器只写入校验后的 8 条预期流，拒绝重复设备流，并在录制中检测接收线程异常；
- 实时订阅启用时钟校正、去抖和单调化，显示实测采样率及元数据偏差；
- POSIX 系统自动将被试资料目录/文件权限收紧为 `0700`/`0600`；
- 调整最小窗口和模块布局，避免设置页控件被裁切；
- 实时监测按 EEG、fNIRS、Motion 和指标流语义重新布局，增加去直流显示、稳健纵轴、EEG 频谱与频带功率；
- 使用 `edge-tts` 预生成并内置四条中文女声 WAV，实验运行时完全离线，并将提示文本和音色写入 Marker；
- 保留 LabRecorder 及其 RCS 工作流作为可选兼容方案。

## 0.5.0

- 新增 macOS 安装、实验启动、实时监测、LabRecorder 启动和自测脚本；
- 默认数据目录、中文字体和系统提示音按 Windows/macOS 自动适配；
- 实验 GUI 可自动定位并打开已下载或已安装的 macOS `LabRecorder.app`；
- 增加 macOS LabRecorder 定位和平台默认值单元测试；
- 文档明确 macOS/Windows 双机 LSL 拓扑，以及 BioMultiLite 仍需运行在 Windows 的设备边界。

## 0.4.1

- LabRecorder 停止录制改用独立的 60 秒响应窗口，避免 XDF 写盘较慢时被 5 秒连接超时误判为保存失败；
- RCS 响应超时改为可读错误并写入结构化诊断日志；
- 修复录制检查页“自动扫描 LSL 数据流”按钮遮挡最后一个准备复选框的问题。

## 0.4.0

- M4A/M4B 使用水杯、手机和药瓶图片作为正式视觉刺激；
- 放大箭头、字母、倒计时、操作说明和物体名称；
- 首页改为“被试与会话 / 实验模块 / 录制检查”三页表单；
- 新增姓名、年龄、性别、受教育年限、惯用手及知情同意记录；
- 姓名只保存在本地受限 profile，不写入 XDF、文件名或逐条 Marker；
- M0 与 N-Back 评分改为全屏内嵌表单，不再逐项弹窗；
- 模块衔接、手动录制与保存结果改为全屏内联操作；
- 新增长静息/休息结束和模块完成的 Windows 扬声器提示音及 Marker；
- 新增 LSL 六类数值流自动扫描和试次无效/设备调整/信号中断快捷 Marker；
- M1 限制同类条件最多连续 2 次，M4A 在每个意图条件内平衡三类物体；
- M2 新增原方案由易到难与按种子拉丁方平衡两种负荷顺序；
- 正式认知/意图任务开始前新增“已完成指导与练习”检查项；
- 修复手动录制中止流程和录制前空事件日志占用 Run 的问题。

## 0.3.0

- 新增 M0、M1、M2、M3A、M3B、M4A、M4B 模块化正式采集协议；
- 支持单独选择模块或按固定顺序连续采集，每个模块独立保存 XDF；
- 每个模块结束后由操作员决定是否进入下一个模块；
- M1 使用可复现、按 Run 平衡的三分类随机序列，并提供老年被试节奏；
- N-Back 自动记录刺激、按键反应时、正确性、疲劳与难度评分；
- M0 问卷与 M4B 目标物体作为结构化 Marker 保存；
- 保留原 device-QC 和短流程联调能力。

## 0.2.5

- Python 发行包名称从 `bsense-lsl-experiment` 统一为 `bsense-lsl`；
- 保留 `bsense_experiment` 导入包和现有命令入口，避免破坏已有脚本；
- Windows 安装时清理虚拟环境中的旧发行包元数据。

## 0.2.4

- Windows 虚拟环境恢复统一使用 `.venv`；
- 安装脚本保留操作系统与 Python 3.13 有效性检查，避免再次使用 macOS 虚拟环境。

## 0.2.3

- 项目元数据支持 Python 3.13；
- Windows 安装脚本固定使用 Python 3.13 和 `.venv-windows`；
- 检测到旧 `.venv` 时明确提示并忽略，避免再次引用其他操作系统的解释器路径。

## 0.2.2

- Windows 脚本改用独立的 `.venv-windows`，避免误用从 macOS/Linux 复制过来的虚拟环境。

## 0.2.1

- 修复默认实时缓冲秒数以浮点数传入 pylsl，导致发现流后接收线程无法创建的问题；
- 实时监测页直接显示 LSL 接收线程错误，不再只显示“等待新样本”。

## 0.2.0

- 新增 BioMultiLite 六类数值 LSL 流的自动发现与并行订阅；
- 新增 EEG、fNIRS、Motion 和生理指标实时波形窗口；
- 新增线程安全的完整通道时间窗接口，供后续分类模型读取；
- 新增独立实时监测入口和 Windows 启动脚本；
- 明确 BioMultiLite、LabRecorder、实时监测与未来设备控制的职责边界。

## 0.1.1

- 新增 LabRecorder RCS `OK` 回执验证；
- 保持单次录制使用同一 TCP 连接；
- 开始任务前验证 XDF 已创建且非零；
- 停止后再次验证 XDF；
- 新增 `_recorder.jsonl` 诊断日志；
- 禁止覆盖已有 XDF 和日志。

## 0.1.0

- 首个 BSense-R device-QC GUI；
- 自动 JSON LSL Marker；
- 短流程与完整流程；
- LabRecorder RCS 自动控制。
